import { NetworkVisualizer } from "@/components/network/network-visualizer";

export const metadata = { title: "Network Lab — Packet & Protocol Workbench" };

export default function NetworkLabPage() {
  return <NetworkVisualizer />;
}
