import { AssemblyVisualizer } from "@/components/assembly/assembly-visualizer";

export const metadata = { title: "Assembly Visualizer" };

export default function AssemblyLabPage() {
  return <AssemblyVisualizer />;
}
